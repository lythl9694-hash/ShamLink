{
  "name": "sanad-finance-platform",
  "version": "0.2.0",
  "private": true,
  "description": "منصة سند المالية لإدارة الوكالات والحوالات",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check dashboard.js && node --check account.js && node --check admin-accounts.js"
  },
  "engines": {
    "node": ">=24"
  }
}
